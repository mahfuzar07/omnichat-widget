type ChatHeaderProps = {
    title: string;
    onClose: () => void;
    primaryColor: string;
};
declare const ChatHeader: ({ title, onClose, primaryColor }: ChatHeaderProps) => import("react/jsx-runtime").JSX.Element;
export default ChatHeader;
